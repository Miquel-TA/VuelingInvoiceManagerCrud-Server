Please check Aditional Files release.

Drop the App.config into the API project.

Create the Microsoft SQL Database named "VuelingInvoiceManager". (You can edit the Connection String if you want to use another DB name or authentication method)

Run the database script.

Run the database view script.

